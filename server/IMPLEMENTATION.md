# 구현 결정 사항

## 인증

- `Authorization: Bearer <token>` 방식
- 토큰은 환경변수 `API_SECRET`에서 읽음
- 인증 실패 시 404 반환 (엔드포인트 존재 노출 방지)

## 저장소

- 위치 이력은 `.data/{id}.json` 파일에 저장
- ID당 최대 50건만 유지 (초과 시 오래된 것부터 제거)
- 조회는 인메모리 캐시에서 반환 (서버 재시작 시 캐시 소멸 감수)
- 쓰기 시 캐시 + 파일 동시 반영
